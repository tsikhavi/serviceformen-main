export interface IProposal {
  id: number;
  name: string;
  profile_id: number;
  place: string;
  min_price: number;
  max_price: number;
  description: string;
  contact: string;
  status: number;
}
